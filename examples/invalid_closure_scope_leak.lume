fn main(args: [str]) -> int {
  let a = [1, 2, 3]
  let b = [4, 5, 6]
  let x = list.map(a, fn(value: int) -> int => value + 1)
  let y = list.map(b, fn(value: int) -> int => value + 2)
  print(value)
  return 0
}
