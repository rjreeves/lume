protocol Named {}

type User {
  name: str
}

impl Named for User {}

fn keep_named<T: Named>(value: T) -> T {
  return value
}

fn main(args: [str]) -> int {
  let user = keep_named(User(name: "Ada"))
  print(user.name)
  return 0
}
