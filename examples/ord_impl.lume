type Money {
  cents: int
}

impl Ord for Money {
  fn compare(a: Money, b: Money) -> int {
    return a.cents - b.cents
  }
}

fn biggest(a: Money, b: Money) -> Money {
  if a < b {
    return b
  }
  return a
}

fn keep_ord<T: Ord>(value: T) -> T {
  return value
}

fn biggerGeneric<T: Ord>(a: T, b: T) -> T {
  if a < b {
    return b
  }
  return a
}

fn main(args: [str]) -> int {
  let a = Money(cents: 100)
  let b = Money(cents: 250)
  print(a < b)
  print(a <= b)
  print(a > b)
  print(a >= b)
  let winner = biggest(a, b)
  print(winner.cents)
  let kept = keep_ord(a)
  print(kept.cents)
  let generic_winner = biggerGeneric(a, b)
  print(generic_winner.cents)
  return 0
}
