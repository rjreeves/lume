enum State {
  ready
  completed(code: int)
}

fn value(state: State) -> int {
  return match state {
    ready => 0
    completed(code, extra) => code
  }
}

fn main(args: [str]) -> int {
  return 0
}
