protocol Named {
  fn name(value: Self) -> str
}

type User {
  name: str
}

impl Named for User {
  fn name(value: User) -> str {
    return value.name
  }

  fn code(value: User) -> int {
    return 1
  }
}

fn main(args: [str]) -> int {
  return 0
}
