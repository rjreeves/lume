enum State {
  ready
  completed(code: int)
}

fn main(args: [str]) -> int {
  let values = [State.completed(code: 1), State.ready()]
  let codes = list.map(values, fn(item: State) -> int => match item {
    ready => 0
    completed(code) => code
  })
  print(code)
  return 0
}
